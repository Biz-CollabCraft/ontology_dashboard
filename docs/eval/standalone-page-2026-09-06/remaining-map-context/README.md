# 미확인 표시 원인과 수정

2026-09-06 현재 사용자 선택 CNC-S04-L02-04 기준.

- 실제 관측: API 프로세스가 중단되어 nginx가 502를 반환했다. 기존 화면 목록은 남고 상세가 비어 예측 구간·추세 등까지 미확인/미산정으로 표시됐다. 기존 설정의 API를 재시작해 복구했다. 프로세스가 중단된 최초 이유는 확인하지 못했다.
- 기존 샘플은 S03-L03 5대에만 연결돼 있었다. 나머지 95대에 동일 계약 참고자료 456건을 추가했다. 기존 데이터나 위험 결과·정비 이력·작업 상태를 덮어쓰지 않았다.
- 적재 백업: /tmp/ontology-before-map-expansion.dump. 대상은 전용 ontology_standalone DB/63542만 사용.
- 30일 유사 사례 누락을 정비 정보 전체 누락으로 번역하던 오류를 수정했다. 가동시간·부하·생산영향 등급 누락도 해당 항목만 표시한다.
- 조건부 생산 계산 실패는 정비 착수 조건 미충족/연결된 재공 정보 없음/계산 조건 미등록으로 원인별 표시한다.
- 화면 샘플 배지를 다시 추가하지 않았다. 출처는 DB/API에 유지한다.

검증: Context 계약 456건 통과, 임시 Context DB 재실행 신규 0건. 실제 PostgreSQL 신규 456건. 현재 선택 CNC-S04-L02-04, CMP-S04-L02-01, CNC-S01-L03-04의 인증 API·직접 URL·새로고침·화면을 검증했다. 생산관리자 계획 200개, 공통 상세 재공 표시와 잘못된 전체 정비 누락 경고가 없는 것을 확인했다. 표시 단위 검사 10개 통과. 브라우저 회귀 결과는 browser-tests.txt 참조(8개 실제 PG, 1개 지연 주입, 2개 mock 저장).

남은 미지원: 실제 가동 상태 집계, 30일 유사 사례 집계, 조치 임계값, 압축기 생산 의존관계, 정비 착수 조건/권한 계약. 계획 샘플로 이 값을 대체하지 않는다. 이 작업은 해당 모든 업무 계약을 구현한 것이 아니다.
