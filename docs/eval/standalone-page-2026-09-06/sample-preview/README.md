# 동일 라인 샘플 적재 미리보기

상태: 2026-09-06 전용 검증 PostgreSQL 적재 및 실제 API·화면 검증 완료. 아래 사전 검증 기록은 적재 전 단계의 결과이다.

## 대상과 출처

- 번들: `DEMO-S03L03-20260829-v1`
- 범위: `org-ontology-demo / manufacturing-demo-project / manufacturing-demo`
- 대상: 실제 현재 목록 API에서 확인한 CMP-S03-L03-01 및 CNC-S03-L03-01~04.
- 기준: 2026-08-29 23:00 한국시간. 각 설비의 정확한 RESULT event/snapshot에 연결.
- 유효기간: 2026-08-29 00:00부터 2026-08-30 00:00 한국시간 미만.
- 예측 목록의 dataset: `dsv-9fc144c7-d3f8-5b37-8465-04248165b7ce`. ContextSnapshot 계약에 dataset 필드는 없으므로 임의 추가하지 않음. Context는 조직·프로젝트·워크스페이스·설비·event/snapshot·as-of로 결합한다.
- 모든 새 생산·계획·품질 항목은 `synthetic_demo_context`, 관계는 `assumed_demo`. 현장 실측이나 소유 시스템에서 받은 자료가 아니다.

## 항목표

| 영역 | 압축기 | 절삭기 4대 | 합계 |
|---|---:|---:|---:|
| 생산 주문·재공 | 빈 목록과 연결 한계 1건 | 설비별 주문/재공 1쌍을 포함한 4건 | 5 |
| 정비 준비 | 미확인 조건 1건 | 미확인 조건 4건 | 5 |
| 품질·납기 | 빈 목록 1건 | 출하 확인 필요 로트·납기 4건 | 5 |
| 선택지 계산 가정 | 가정 없음 1건 | 정지/계속 가동 비교 가정 4건 | 5 |
| 생산계획 | 미등록 | 설비별 계획 4건 | 4 |
| 총 ContextSnapshot | 4 | 20 | 24 |

압축기 생산 의존관계가 계약에 등록되지 않았다. 해당 설비에 절삭기 생산량을 붙이거나 공용 라인 계획을 중복 합산하지 않는다. 압축기 계획/생산 영향은 미확인·미산정으로 유지한다.

## 반영 후 확인할 예상 값

| 표시 항목 | 절삭기 한 대 | 라인 합계 또는 주의사항 |
|---|---|---|
| 시연 생산계획 | 200개 | 4대 합계 800개, 압축기 제외 |
| 시연 완료 수량 / 재공 | 150개 / 50개 | 600개 / 200개. 실제 실적 아님 |
| 가정한 1시간 정지의 계획 영향 | 25개 | 60분 ÷ 60 × 25개/시간. 예측된 정지시간 아님 |
| 즉시 정지 선택지의 잔여 재공 영향 | 50개 | 생산 가능 수량 0, 대체 설비 없음이라는 가정 |
| 계속 가동 선택지의 잔여 재공 영향 | 0개 | 잔여 50개 생산 가능 가정. 안전·품질·출하 승인 또는 추천을 의미하지 않음 |
| 정비안 선택지 | 미산정 | MAINTENANCE_BLOCKED: 준비·착수 조건 미확인 |
| 압축기 선택지 전체 | 미산정 | MISSING_WIP: 생산 의존관계/재공 미등록 |
| 품질 | 확인 필요, 출하 확인 필요 | 품질 합격/출하 승인 샘플을 생성하지 않음 |

계획 영향 25개와 재공 영향 50개는 다른 지표이다. 프론트에서 하나의 손실량으로 합치지 않는다. 설비 위험 등급·차트·예측 결과·기존 정비 이력은 이 번들에서 변경하지 않는다. WorkOrder, 승인, 정비 완료 레코드는 생성하지 않는다.

## 검증 결과와 한계

- 공식 importer dry-run: `validated=24, applied=false`.
- 임시 SQLite Context 저장/조회 미리보기: 최초 24건, 재실행 신규 0건/동일 24건.
- 서버 read_view로 5대의 domain 상태와 선택지 계산 결과를 생성했다. 상세 출력은 `expected-context.json`.
- 이 SQLite 검증은 Context 계약의 사전 검증이다. 예지보전 PostgreSQL runtime 또는 브라우저 연동 검증을 대체하지 않는다.
- `available`은 Context 문서 조회 성공이다. 정비 가능·품질 통과·작업 허가를 의미하지 않는다.
- 이번 단계에서 화면 캡처를 새로 만들지 않았다. 샘플 적용 전 화면을 적용 후 결과로 제시하지 않는다.

## 재생성 및 다음 실행 단계

저장소 루트에서 `PYTHONPATH=systems/backend:ml/src .venv/bin/python docs/eval/standalone-page-2026-09-06/sample-preview/build_preview.py`로 재생성한다. 이 스크립트는 실행 중인 DB에 접속하지 않는다.

다음 적재 단계에서는 전용 검증 PostgreSQL의 대상 DB와 기존 건수를 확인하고 백업한 뒤, 기존 importer에 이 manifest와 `--allow-demo --apply`를 전달한다. 재실행 신규 0건을 확인한다. 삭제 API는 아직 준비하지 않았으므로 실패 시 검증 DB 백업 복원으로 되돌린다. 다른 작업자의 DB를 변경하지 않는다.

그 다음 목록→동일 snapshot 상세→operational-context→화면 표시를 대조하고, 새로고침/설비 전환으로 값이 섞이지 않는지 확인한다. 한국어·시연 출처 표시·기존 디자인을 확인한 캡처를 남긴다. 샘플 적재로 기존 미지원 업무 버튼을 활성화하지 않는다.


## 실제 PostgreSQL 반영 결과 (2026-09-06)

- 대상: 로컬 전용 `ontology-standalone-page-pg`, DB `ontology_standalone`, 포트 63542. 다른 작업자의 integration DB는 변경하지 않았다.
- 적재 전 전체 백업: `/tmp/ontology-before-sample-20260906.dump` (임시 파일이며 자동 삭제될 수 있음).
- 공식 importer 적용: validated 24 / inserted 24 / unchanged 0. 동일 파일 재적용: inserted 0 / unchanged 24.
- 5대의 인증된 실제 API 응답에서 정확한 설비·snapshot·as-of 및 synthetic 출처를 확인했다. 새로고침 후 설비 선택이 유지됐다.
- 생산관리자 화면에서 계획 200개, 시연 1시간 정지 가정에 따른 영향 25개를 확인했다. 공통 상세에 주문 수량·주문 완료 수량·재공 수량을 연결했다. 주문 완료는 교대별 실적이 아니므로 교대 실적 영역의 미확인은 유지한다.
- 메인 상단에 생산·정비 참고자료의 시연용 출처 표시, 생산 차질 수치에 시연 가정 표시를 보강했다. 기존 레이아웃은 유지했다.
- 압축기의 생산 영향은 계속 미산정이며 작업 실행 버튼은 활성화하지 않았다.
- 실제 API/표시 근거: `live-api-ui.json`, `live-verification.txt`. 빌드: `build.txt`. 브라우저 회귀: `browser-tests.txt` (8개 실제 PG, 1개 응답 지연 주입, 2개 mock 저장). mock 저장은 실제 서버 업무 저장 검증이 아니다.
- 캡처: `production-plan.png`, `context-sideview.png`, `CNC-S03-L03-03.png`, `CMP-S03-L03-01.png`.
- 실행 중인 메인 URL: http://127.0.0.1:13311/. 같은 라인 절삭기 선택 후 생산관리자 탭에서 샘플 계획을 확인한다.
- 전용 서버 외 배포·커밋·push는 하지 않았다.

## 표시 정책 변경

사용자 요청에 따라 화면의 시연용·시연 자료·시연 계획 반복 표시를 제거했다. 품목은 부품 A로 표시한다. API/DB의 synthetic 출처는 그대로 보존하고, 정지시간 가정 및 미확인·미산정 표시는 유지한다. 이전 캡처는 표시 변경 전 기록이다. 프론트 빌드 통과 및 현재 탭 새로고침 완료.
