# 에이전트 검수 — 2026-09-14

사용자의 요청으로 두 하위 에이전트가 기준과 라벨을 별도로 검수했다. 두 에이전트는 과거 모델 비교 결과 파일을 읽지 않았다고 보고했다. 같은 작성 환경과 자료를 공유한 AI 검수이며 사람의 독립 검수나 도메인 정답 인증이 아니다. 원문 48건, 제안 라벨 44건, 미정 4건을 대상으로 했다. 대상 candidates.json SHA256은 `fbfb8c6be3ecd69bd047b9d59a821ca181f55868ac92a97e67544fff0d0395bc`다.

## 공통 결론

- rubric_review: 정보 누락·측정 상태 제안 라벨 44건에서 확정적인 기준 위반을 발견하지 못했다.
- label_consistency: 44건의 제안 라벨과 행동 제약이 현재 기준에 일치한다. 수정 5건 D03-04, D06-04, H03-04, H04-04, H06-04도 수용 가능하다.
- 정보 누락을 원문에 명시된 기록/값의 부재·미확인으로 한정하는 기준, optional과 not_required 구분은 타당하다.
- unclear는 측정 필수가 아니다. 현재 모호 지시 10건은 measurement_required=false이며 확인 보류로 이어진다.

44/44는 에이전트가 확정적 기준 위반을 발견하지 못한 범위다. 44개 정답의 정확성 인증이나 모델 평가 점수가 아니다.

## 적용 범위와 남은 문제

1. 모호한 지시와 별도의 명시적인 필수 측정이 한 원문에 함께 있으면 단일 meaning/measurement_status에 두 의미를 어떻게 보존할지 정해져 있지 않다. 현재 사례는 충돌+측정 조합만 포함한다. 프롬프트 문구만으로 해결됐다고 주장하지 말고 복합 문장 처리·출력 표현을 별도 설계/검증할 필요가 있다.
2. 모호한 지시라도 가능한 해석이 모두 명확한 비측정 작업이라면 measurement_status=not_stated가 가능해야 한다. 모든 모호함을 측정 모호함으로 일반화하지 않는 제한 문장을 후속 기준·프롬프트에 반영할 수 있다. H04-04는 준비 작업의 내용이 불명이라 현재 라벨을 유지할 수 있다.
3. 교육·참고라는 목적 자체가 optional을 뜻하지 않는다. 명시적인 필수 요구가 있으면 required가 우선한다. 현재 D05-01/H05-01에는 비필수 진술도 있으므로 수정할 필요가 없다.

## 미정 사례에 대한 의견 차이

| 사례 | label_consistency | rubric_review | 이번 처리 |
|---|---|---|---|
| D05-03/H05-03 | 측정 조건의 현재 값이 없고 조건부 상태가 정의되지 않았으므로 미정 유지 | 명시 조건부 측정 2건으로 구분 | 미정 유지 |
| D05-04/H05-04 | 작업 자체가 불명확하므로 ambiguous_request/unclear로 제안 가능 | 조건·작업 미정으로 따로 설명하면 충분하며 unresolved 유지도 허용 | 의견 차이를 기록하고 미정 유지 |

두 번째 그룹에 대한 제안은 information_missing=false, unresolved_conflict=false, uncertain=true, measurement_required=false, 선행 확인 보류다. 두 에이전트가 미정 해소에 합의한 것은 아니다.

## 로컬 코드 대조와 수행 범위

현재 Agent의 보류/진단 처리는 unresolved_conflict, uncertain, measurement_required를 사용한다. information_missing 자체로 행동을 바꾸지는 않는다. 이 사실은 정보 누락 분류 품질을 보증하지 않지만 불일치의 현재 영향 범위를 설명한다. 런타임은 단일 meaning과 measurement_status에서 두 파생 플래그를 계산하므로 복합 요청 조합의 표현 한계는 실제 코드와도 관련된다.

기존 구조 검증은 오류 0, 제안 44/미정 4/검수 pending 48이다. 이번 작업은 검수 기록만 추가했다. 데이터 라벨·프롬프트·서버·승인 상태 변경, 추가 모델 비교 API 호출, 재채점, commit/push는 하지 않았다.

현재 범위의 다음 작업은 합의된 일반 기준을 프롬프트에 전달하고 회귀를 확인하는 것이다. 미정 조건부 사례와 복합 지시의 처리까지 해결됐다고 보지는 않는다.
