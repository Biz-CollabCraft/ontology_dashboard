# 최종 안정성 평가 — 2026-09-08

## 판정

**로컬 시연 경로: 통과. 현재 설정의 팀 DB 조회: 통과. 운영 전체 경로: 미검증.**

별도 배포 DB가 명시되지 않아 기존 연결 설정의 팀 DB를 대상으로 읽기 전용 점검했다. 운영 데이터나 업무 상태를 변경하지 않았으며 DB 데이터를 외부 LLM으로 전송하지 않았다.

## 평가 대상

- 병합된 `origin/demo`: `ab63cfe9cbfa96414ab5a7d5bfd0321c35df8372`
- 평가 작업 트리 HEAD: `2219093930fa8a1e0b887f98217f267eb505d1c0`
- 두 Git tree 내용 일치, 작업 트리 깨끗함.
- PR #168 병합 완료. 원격 검사 3개 모두 성공.

## 결과

| 구분 | 결과 | 검증 범위 |
|---|---|---|
| 원격 서버 회귀 | 416 passed, 2 skipped, 2 warnings | PostgreSQL 포함, 기존 CI 실패 2개 수정 확인 |
| 현재 코드 계약 재실행 | 28 passed | 입력 전달·기여·유용성 평가·실제 연결·replay |
| 실제 브라우저 replay | 24/24 | 8개 입력 × 3개 관점, 초기 표시 효과·재생 방지·fallback |
| 브라우저 오류 대응 | 16/16 | 중복 클릭·지연·손상 응답·선택 전환·재시도·너비 |
| 로컬 예측부터 화면 연결 | 24/24 및 실패 차단 | 실제 규칙 기반 예측·워크플로우·임시 SQLite·제품 조회 API·브라우저; 외부 LLM 전송은 통제 응답 |
| 팀 DB 읽기 일관성 | 100대 × 3회 동일 | 동일한 repeatable-read 트랜잭션, 기준 시각 이하 최신 기록 |

브라우저 실행 오류 0. DB 조회 결과의 미래 기록 0, 식별자 누락 0. 세 조회의 해시가 일치했다. 이는 동일 트랜잭션의 읽기 일관성이며 장애 복구·동시 쓰기·고부하 측정이 아니다.

## 팀 DB의 데이터 경계

- 읽기 전용 트랜잭션과 RLS 유지. superuser/우회 권한 없음.
- 예측 기록 181,741개, 설비 100대, 모델 표기 `independent-logreg-v3.1`.
- 점검 시점 전체 테이블에 미래 관측이 포함돼 있었다. 최신 현재 상태 조회에서는 기준 시각 조건을 적용했다.
- 운영 문맥은 `synthetic_demo_context`이며 현재 조회 범위의 저장 AI 요약은 0개.
- 이번 점검에서 운영 모델을 재실행하거나 AI 요약을 공유 DB에 생성하지 않았다.

## 실제 LLM 결과와의 구분

앞선 승인된 고정 입력 2개 실제 gpt-5.6-luna 호출은 각각 피드백 재생성 후 최종 통과했고 역할별 포맷터 6/6을 확인했다. 이 수치를 운영 DB 기반 생성이나 배포 환경 전체 연결의 결과로 합산하지 않는다. 이번 평가는 새 외부 LLM 호출 없이 진행했다.

## 남은 운영 검증

배포 환경의 읽기 API·화면 주소와 대상 설비/사건 범위를 지정한 뒤 동일 기준 시각에서 DB → 근거 패키지 → 실제 생성·검증 → 저장·조회 → 배포 화면의 일치를 확인해야 한다. 실제 운영 상태 변경, 외부 전송, 부하 시험은 각각 해당 범위가 확인된 환경에서 수행한다.

실제 현장 사용자 평가와 운영 KPI는 측정하지 않았다.

[원격 서버 회귀 실행](https://github.com/Biz-CollabCraft/ontology_dashboard/actions/runs/34187276079) · [병합 PR #168](https://github.com/Biz-CollabCraft/ontology_dashboard/pull/168)
