# P0 independent oracle notes

## Scope and ownership
Read AGENTS.md, the engineering workflow, and the approved 2026-09-23 measurement plan. Human hypothesis remains unrecorded; Human decision and Changed belief are user-owned. No product changes, paid provider calls, or load execution by this reviewer.

## Current-code contract discrepancy
`systems/backend/app/operations/service.py::cached_agent_review_summary_for_packet` performs exact validated lookup, then on a miss calls `latest_agent_review_summary` scoped by organization/project/workspace/asset/event/history. It returns that historical summary with `reuse_eligibility=LATEST_STORED` and `latest_stored=true`, without validating it against the current packet. `router.py::get_agent_review_summary` returns HTTP 200 whenever a summary exists. `tests/test_operations.py::test_agent_review_summary_lookup_keeps_latest_stored_when_snapshot_moves` explicitly expects this behavior and preservation of the old summary key/decision_as_of. This was confirmed by the SQLite/in-process API diagnostic `test_oracle_boundary.py`: HTTP 200, LATEST_STORED, unchanged old summary/key, different requested exact key, zero GET provider invocations. `oracle-api-diagnostic.txt` preserves raw output. Existing tests produced one pass for intentional stored-history behavior and one failure for the older strict miss expectation (`oracle-existing-tests.txt`). A first test attempt failed fixture setup because APP_ENV=test disables heuristic fallback; the evidence run explicitly enabled the synthetic heuristic predictor. Parent independently inspected frontend NaturalBriefing.tsx: it labels LATEST_STORED as a briefing from an earlier business time and uses original decision_as_of. This is a strict-plan contract discrepancy, not evidence of hidden rebinding or mislabeled current prose.

The plan's exact requested-snapshot invariant is stricter than this stored-history behavior. A same-event changed-context probe is needed. A new unique event-only sequence cannot falsify this boundary because repository fallback filters event_id. Do not call LATEST_STORED exact reuse or consumer-ready for the new identity. Preserve old provenance; distinguish disclosed historical output from rebinding old prose to a new identity.

## Independent evidence requirements
- GET generation: attribute provider boundary invocation to request context/thread. Concurrent global before/after call-count deltas are invalid attribution. Distinguish GET-triggered provider calls from cached traces containing historical generation_metrics.
- Snapshot/scope: compare fixture/admitted input expectation with returned summary and materialization provenance, not two fields derived from the returned object. Capture requested and returned event, asset, dataset, source, observed_at, organization, project, workspace, history and exact key where independently available. Record missing observation as unknown, not zero violations.
- Side effects: compare protected business table digests before/after. Explicitly allow admitted synthetic input tables and materialization/workflow records only. Equal row counts alone miss updates/deletes+inserts. Digest coverage and exclusions must be reported.
- Latest-only: offered events are distinct from generation starts. Keep terminal superseded, unobserved-between-scans, deferred, failed, fallback, exact ready, latest-required pending distinct. Old event-bound generation completing after a newer event is not itself a snapshot error. Current service loader reloads that selected event.
- Duplicates: group boundary calls by exact key and classify intentional refresh, fallback retry, timeout recovery separately. Provider boundary invocation is not an HTTP billing count. Cached trace generation_metrics is historical and cannot be summed as new work.
- Timing: monotonic clock; explicit warmup/measurement/drain bounds; planned warmup >=2 poll cycles and measurement >=max(3 poll cycles,180 seconds), three repeats per representative scenario. Completed-only E2E p95 is censored; report n and unfinished count/age alongside it. Query scheduling must record offered versus started to expose coordinated omission.
- Stop gates: 30s no completions must define eligible completion and avoid applying during expected no-work/poll idle time without disclosure. Errors need injected/technical separation. No capacity generalization beyond topology and measured duration.

## Harness review before bounded PostgreSQL preflight
Reviewed runner.py and fixture.py. Context mutation appends immutable production owner snapshots through OperationalContextRepository.import_snapshots, retaining the same admitted Product Result event and choosing effective source times before event decision time. The guarded generation reloads this event through runtime_agent_review_packet. Effectiveness must be shown by actual before/after exact-key differences for both mutations. Provider start uses a threading event; GET runs while the provider sleeps. Storage checks must show the invalidated in-flight key absent, newest key absent unless subsequently generated, and worker joined. Intentional context inputs are excluded from the nine protected closed-loop business table digests. Review found and harness fixed drain classification reading nested generation_policy.reuse_eligibility instead of top-level reuse_eligibility. Normal/slow throughput conclusions require full observation windows; this bounded probe cannot supply them.

## Known limitations to retain
Provider double evaluates timing/contracts, not prose quality, actual HTTP retries or cost. Service/in-process API tests are not socket/deployment latency. One process does not establish multi-process lease/crash guarantees. Historical prose with honest provenance is still not fresh exact readiness. No production SLA has been supplied.
