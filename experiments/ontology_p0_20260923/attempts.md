# Harness validity attempts

- preflight-01 (ontology_p0_preflight): fixture invalid, GET ValueError `Result Artifact provenance must assert canonical_source_mutated=false`; watcher failed twice with result artifact not found; zero provider calls. Raw retained. This is not product failure evidence.
- preflight-02 (ontology_p0_preflight02): empty raw retained; tool initially exposed only INVALID_ARGUMENT, exact traceback not retained. Retry refused overwrite via FileExistsError.
- preflight-02b (same disposable DB): setup succeeded, first synthetic event validation failed: `Product Result Artifact schema validation failed: provenance/source_type: 'product_runtime_inference' was expected`. No event admission/provider calls. Empty raw retained. Fixture correction follows validated existing schema; product unchanged.
- preflight-02c: valid Product Result HTTP202 and ten live candidates reached policy, but input deferred INVALID_INPUT because fixture payload dataset_version (canonical-independent-v1.0) disagreed with query dataset_version_id (dsv UUID). No provider calls; context1 import effective; delayed probe correctly failed provider-start precondition. Fix fixture provenance identity, not application policy.
- preflight-03 (fresh ontology_p0_preflight03): valid fixture established10 baseline provider calls and HTTP200 EXACT_VALIDATED after HTTP202 miss. Two real context changes both changed materialization key. One10s provider generation raced context2 import, rejected publication with snapshot-binding error; neither changed-context key stored. HTTP GET remained independent and returned explicitly historical LATEST_STORED after context changes. Raw complete with before/after business snapshots. This is bounded causal preflight, not sustained P0 workload or three-repeat baseline.
