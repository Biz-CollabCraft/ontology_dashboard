import {it,expect} from 'vitest';
import {selectedBrief,briefContext,equipmentName,eventBrief} from './fieldBrief.js';
it('builds screenshot-style evidence sentences using source values',()=>{const d={event:{failureProbability:.72,criticality:'high'},threshold:.6,topFactors:[{feature:'torque_nm_current',direction:'risk_up'}],maintenanceContext:{lastMaintenanceDaysAgo:26,similarEvents30d:3,openWorkOrderExists:true},predictionHorizonHours:48};const l={work_orders:[{status:'in_progress'}]};const prose=selectedBrief(d,l).flatMap(x=>x.parts.map(p=>p.text)).join('');expect(prose).toContain('위험 점수 0.72');expect(prose).toContain('기준 0.60');expect(prose).toContain('26일');expect(prose).toContain('3건');expect(prose).toContain('미종결 작업지시');expect(prose).not.toContain('2시간 30분');expect(briefContext(d,l,{assetId:'CNC-S03-L03-01'}).map(x=>x.value)).toEqual(['26일','3건','1건','높음','48시간','절삭기']);});
it('does not invent thresholds or maintenance facts when unavailable',()=>{const prose=selectedBrief({event:{failureProbability:.82},topFactors:[]},null).flatMap(x=>x.parts.map(p=>p.text)).join('');expect(prose).toContain('조치 기준 미등록');expect(prose).toContain('최근 정비일 미등록');expect(prose).not.toContain('26');});
it('maps friendly names while retaining event identifiers outside labels',()=>{const a={assetId:'CNC-S03-L03-01',assetType:'cnc'};expect(equipmentName(a,true)).toBe('절삭 1호');expect(eventBrief({status:'warning',observedAt:'2026-08-29T14:00:00Z'},a).place).toBe('3라인 · 3셀 · 절삭기 1호');expect(a.assetId).toBe('CNC-S03-L03-01');});
