# Backend System Environment Configuration Example
PORT=8000
HOST=0.0.0.0
# Versioned Model Artifact provider. Do not depend on a sibling generator path.
# Local path, mounted volume, or a provider URI may be injected per deployment.
MODEL_ARTIFACT_URI=/var/lib/ontology-dashboard/model-artifacts
LOG_LEVEL=INFO
